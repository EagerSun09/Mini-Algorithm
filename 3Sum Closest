Input: nums = [-1,2,1,-4], target = 1
Output: 2
Explanation: The sum that is closest to the target is 2. (-1 + 2 + 1 = 2).

class Solution {
    public int threeSumClosest(int[] nums, int target) {
        Arrays.sort(nums);
        //info of array
        int len = nums.length;
        int close = Integer.MAX_VALUE;
        int res = 0;
        for(int i=0; i<len-2; i++){
            //two pointer
            int j = i+1;
            int k = len-1;
            //before we walk through whole array
            while(j<k){
                int sum = nums[i] + nums[j] + nums[k];
                int diff = Math.abs(target-sum);
                //if closer
                if(diff < close){
                    close = diff;
                    res = sum;
                }
                //if sum itself is bigger/smaller/equal to target
                if(sum>target){
                    k--;
                }else{
                    j++;
                }
            }
        }
        
        return res;
    }
}
