Given a list of 24-hour clock time points in "HH:MM" format, return the minimum minutes difference between any two time-points in the list.

class Solution {
    public int findMinDifference(List<String> times) {
        //convert all time into minutes
        //calculate the difference
        int[] ts = new int[times.size()];
        for(int i=0; i<times.size(); i++){
            String time = times.get(i);
            ts[i] = 60*Integer.valueOf(time.substring(0,2)) + Integer.valueOf(time.substring(3,5));
        }
        Arrays.sort(ts);
        int res = Integer.MAX_VALUE;
        for(int i=0; i<times.size()-1; i++){
            int min1 = ts[i];
            int min2 = ts[i+1];
            
            res = Math.min(res,min2-min1);
        }
        //calculate from last one to first time
        res = Math.min(res,Math.abs(ts[0]+(24*60-ts[ts.length-1])));
        return res;
    }
}
