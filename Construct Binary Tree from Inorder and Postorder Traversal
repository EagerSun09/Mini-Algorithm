Given two integer arrays inorder and postorder where inorder is the inorder traversal of a binary tree and postorder is the postorder traversal of the same tree, construct and return the binary tree.

 /**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    //first, the last element in postorder is the root
    //then, we just need to know the start and the end position of left/right subtree of root
    HashMap<Integer,Integer> inMap = new HashMap<>();
    public TreeNode buildTree(int[] in, int[] pos) {
        for(int i=0; i<in.length; i++){
            inMap.put(in[i],i);
        }
        return helper(in, 0, in.length-1, pos, 0, pos.length-1);
    }
    
    public TreeNode helper(int[] in, int inStart, int inEnd, int[] pos, int poStart, int posEnd){
        if(inStart>inEnd || poStart>posEnd){
            return null;
        }
        
        TreeNode root = new TreeNode(pos[posEnd]);
        
        int gap = inMap.get(pos[posEnd]);
        root.left = helper(in, inStart, gap-1, pos, poStart, poStart+gap-inStart-1);
        root.right = helper(in, gap+1, inEnd, pos, poStart+gap-inStart, posEnd-1);
        
        return root;
    }
}
