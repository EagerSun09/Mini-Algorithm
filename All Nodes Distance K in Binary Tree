Given the root of a binary tree, the value of a target node target, and an integer k, return an array of the values of all nodes that have a distance k from the target node.

You can return the answer in any order.

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */
class Solution {
    List<Integer> res = new ArrayList<>();
    HashMap<TreeNode, TreeNode> map = new HashMap<>();
    HashSet<TreeNode> seen = new HashSet<>();
    public List<Integer> distanceK(TreeNode root, TreeNode target, int k) {
        //helper1 helpes us to pair nodes with their parents
        helper1(root);
        //helper2 helpes us to get answer
        helper2(target, k);
        
        return res;
    }
    
    public void helper1(TreeNode node){
        if(node == null)
            return;
        
        if(node.left != null){
            map.put(node.left, node);
            helper1(node.left);
        }
        
        if(node.right != null){
            map.put(node.right, node);
            helper1(node.right);
        }
    }
    
    public void helper2(TreeNode node, int d){
        //if seen or empty node
        if(node == null || seen.contains(node))
            return;
        
        seen.add(node);
        //if fulfill the requirement
        if(d == 0){
            res.add(node.val);
        }
        //go to other nodes
        helper2(node.left, d-1);
        helper2(node.right, d-1);
        helper2(map.get(node), d-1);
    }
}
