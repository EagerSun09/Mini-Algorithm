Given the root of a binary tree and an integer targetSum, return the number of paths where the sum of the values along the path equals targetSum.

The path does not need to start or end at the root or a leaf, but it must go downwards (i.e., traveling only from parent nodes to child nodes).

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    //its pretty similar to the sum of contiguous subrray to k
    int count = 0;
    HashMap<Integer, Integer> map = new HashMap<>();
    public int pathSum(TreeNode root, int targetSum) {
        map.put(0, 1);
        helper(root, 0, targetSum);
        return count;
    }
    
    public void helper(TreeNode root, int cur, int t){
        if(root == null){
            return;
        }
        
        cur += root.val;
        if(map.containsKey(cur-t)){
            count += map.get(cur-t);
        }
        map.put(cur, map.getOrDefault(cur,0)+1);
        helper(root.left, cur, t);
        helper(root.right, cur, t);
        //we need to delete current sum cause we can only go downwards
        map.put(cur, map.getOrDefault(cur,0)-1);
    }
}
