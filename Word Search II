Given an m x n board of characters and a list of strings words, return all words on the board.

Each word must be constructed from letters of sequentially adjacent cells, where adjacent cells are horizontally or vertically neighboring. The same letter cell may not be used more than once in a word.

//using trie tree to store strings in word, then use dfs to check if contains word in board

class TrieNode{
    String word;
    TrieNode[] children = new TrieNode[26];
    public void TrieNode(){}
    
}

class Solution {
    List<String> res = new ArrayList<>();
    public List<String> findWords(char[][] board, String[] words) {
        TrieNode root = new TrieNode();
        for(String word:words){
            TrieNode temp = root;
            for(int i=0; i<word.length(); i++){
                char cur = word.charAt(i);
                if(temp.children[cur-'a']==null){
                    temp.children[cur-'a']=new TrieNode();
                }
                temp = temp.children[cur-'a'];
            }
            temp.word = word;
        }
        for(int i=0; i<board.length; i++){
            for(int j=0; j<board[0].length; j++){
                helper(board, i, j, root);
            }
        }
        
        return res;
    }
    
    public void helper(char[][] b, int x, int y, TrieNode root){
        if(root == null){
            return;
        }
        if(root.word != null){
            res.add(root.word);
            root.word = null;
        }
        if(x<0 || y<0 || x>b.length-1 || y>b[0].length-1 || b[x][y]=='#'){
            return;
        }
        
        
        char c = b[x][y];
        b[x][y] = '#';
        
        root = root.children[c-'a'];
        
        helper(b, x-1, y, root);
        helper(b, x, y-1, root);
        helper(b, x+1, y, root);
        helper(b, x, y+1, root);
        
        b[x][y] = c;
    }
}
