Given an integer n, return the number of prime numbers that are strictly less than n.

class Solution {
    public int countPrimes(int n) {
        //we just mark all nom-primes with true
        int count = 0;
        boolean[] notPrime = new boolean[n];
        for(int i=2; i<n; i++){
            if(!notPrime[i]){
                for(int j=2*i; j<n; j+=i){
                    notPrime[j] = true;
                }
            }
        }
        
        for(int i=2; i<n; i++){
            if(!notPrime[i]){
                count++;
            }
        }
        return count;
    }
}
